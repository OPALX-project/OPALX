/* Name of package */
#define PACKAGE ${PACKAGE}

/* Define to the address where bug reports for this package should be sent. */
#define OPAL_PACKAGE_BUGREPORT "${OPAL_PACKAGE_BUGREPORT}"

/* Define to the one symbol short name of this package. */
#define OPAL_PACKAGE_NAME "${OPAL_PACKAGE_NAME}"

/* Define to the version of this package. */
#define OPAL_PACKAGE_VERSION "${OPAL_PACKAGE_VERSION}"

/* Define DKS version used in this package. */
#define DKS_VERSION ${DKS_PACKAGE_VERSION}
